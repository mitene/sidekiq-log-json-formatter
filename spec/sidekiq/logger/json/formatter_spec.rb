RSpec.describe Sidekiq::Logger::Json::Formatter do
  it "has a version number" do
    expect(Sidekiq::Logger::Json::Formatter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
